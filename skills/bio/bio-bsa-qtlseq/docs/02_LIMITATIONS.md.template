# 局限性声明 (LIMITATIONS)

> 本文件系统列出本次 BSA 分析的局限。**客户下游决策请务必阅读全文**。

## 1. 统计力局限 🔴 Critical

### 1.1 混池规模偏小
- 本项目 **bulkSize = 20 个体/池**
- QTL-seq 方法学论文 (Takagi et al 2013, Plant J 74:174) 推荐 **≥ 50 个体/池**
- QTLseqr 论文 (Mansfeld & Grumet 2018, Plant Genome) simulation 基于 **bulkSize=250**
- 理想规模 **≥ 400 个体/池**

### 1.2 症状
- QTL-seq 99% CI 下 **0 区间显著** — 统计力不足的典型表现
- Gprime q=0.01 下 **0 区间显著**
- 14/19 染色体同时出信号 — 符合小样本假阳性分散的预期
- 平均 QTL 长度 3.1 Mb — 远超"可用于基因克隆"的 sub-Mb 标准

### 1.3 影响
- 识别到的区间仅可视作 **候选信号 (candidate intervals)**，**不是已定位的 QTL**
- 进入基因克隆需经：扩群 (≥ 150 F2) → 加密标记 → 单 QTL 效应验证
- 功效不足直接导致 chr11 qtl5 的 11 Mb 伪宽峰（见 §4.1）

---

## 2. 阈值 fallback 🔴 Critical

### 2.1 主阈值降档
预设主阈值 **Gprime q=0.01** 与 **QTLseq 99% CI** 均**无显著区间**。

交付的 20 个 Gprime 候选区间使用 **Gprime q=0.05 (FDR 5%)** fallback 阈值；6 个 QTLseq 精细化区间使用 **QTLseq 95% CI** fallback。

### 2.2 假阳性预期
- FDR 5% → 20 个区间中**预期约 1 个是假阳性**
- 建议用 QTLseq 95% CI 的 6 个区间做交集筛选：被两种方法共同检出的区间（如 chr5 qtl16、chr3 qtl14）可信度更高

### 2.3 Tier 分级建议操作（v2.1 修订）

基于 `meanQval` 实际阈值（见 `03_COLUMN_DEFINITIONS.md §1`）:

- **A 级 (meanQval < 0.025, 6 个)**: qtl7(chr12), qtl16(chr5), qtl9(chr14), qtl14(chr3), qtl5(chr11)*, qtl8(chr13)
- **B 级 (0.025 ≤ meanQval < 0.035, 12 个)**
- **C 级 (0.035 ≤ meanQval ≤ 0.05, 2 个)**

*注: qtl5(chr11) 虽达 A 级 meanQval=0.0240，但在 QTLseq 95% CI 下**未获独立支持**（11.16 Mb 宽峰不符合精细化 QTL 特征），**建议从候选列表剔除**（见 §4.2）。

**实质可用 A 级 = 5 个**。

### 2.4 建议操作
- 优先投入下游资源到 A 级区间
- 不要将 q=0.05 交付基因列表直接用于"候选基因克隆"说法
- Top 区间（chr12 qtl2, 64 kb）即使最窄，也需要标记验证和功能筛选，**不可直接做候选基因克隆**

---

## 3. 方法学边界 🟡 Major

### 3.1 pseudo-pool vs 物理混池
- 本项目 40 个体**逐个测序**后 AD/DP 按池**求和**，不是物理 DNA 混池测序
- 隐含假设：个体间建库/测序/PCR 偏好**均一**
- 实际：若某个体覆盖深度 2-3× 高于均值，会在池中**过度代表**
- 若个体间 DP 差异大，等效 bulkSize 会降到 10-15（进一步削弱统计力）
- **未做 per-sample DP 均一性 QC**（见 `figures/00_diagnostic.pdf` DP 分布初步检视）

### 3.2 GQ 过滤禁用
- 池合并后 GQ 被硬编码为占位值 99
- R 端 `minGQ=0` 实质上**禁用 GQ 层过滤**
- 质量控制实际由 DeepVariant FILTER=PASS + DP/REF_FRQ 阈值承担
- **未单独做 per-sample GQ 筛选**

### 3.3 refAlleleFreq=0.20 偏离默认
- QTLseqr vignette 默认 `refAlleleFreq=0.35`，本项目用 0.20 更宽松
- 已做敏感性分析（`results/sensitivity/refAF_035/`）验证主候选 chr5 qtl16 在 0.35 下仍稳健
- 但保留更多低 AF 位点（可能含测序错误）是弱点

### 3.4 亲本 GT 判定
- 仅保留 Va/Vv **纯合且差异**位点
- half-call (`GT=(0,)`) 在 v2 版本已被独立计数并排除
- `min-parent-dp=5` 对 DeepVariant 低覆盖位点的 0/0 call 略宽松；DeepVariant 在 DP<10 时假阳性 call 率上升

### 3.5 敏感性分析结果的方向性（v2.1 新增）

完整 4×3 敏感性矩阵见 `results/main/SUMMARY_v2.md`:

| 参数 | Gprime q001 | Gprime q005 | QTLseq ci99 | QTLseq ci95 |
|------|-------------|-------------|-------------|-------------|
| 主 | 0 | 20 | 0 | 6 |
| w500kb | 23 | 42 | 0 | **18** |
| rf035 | 0 | 19 | 1 | 7 |

**关键解释**:

- **w500k q001=23 vs 主=0**: 窗口从 1 Mb 降到 500 kb 后，每窗 SNP 减半，G' permutation null 分布方差增大，尾部变厚，q<0.01 阈值**更易触及**。这是**统计伪象**，不代表窗口缩小后分析更严格。
- **w500k ci95=18 vs 主=6**: 窗口变窄导致 CI 带宽变小（bootstrap SNP 数减少），更多区间超出 CI。**不等价于"主分析保守"**。
- **w500k 与主分析的区间不直接可比**。敏感性分析的作用是**验证主候选 chr5 qtl16 在多组参数下是否稳健**（答案：✓ chr5 峰位 25,021,533 bp 在 3 组参数下均为最强）；**不是**取敏感性分析的区间数当作最终结论。
- **chr11 qtl5 的 11.16 Mb 宽峰在 w500k 下仍存在**（约 11.30 Mb, q=0.006 甚至比主分析更显著），但峰仍未收缩成 <1 Mb 细窗（见 §4.2）。

---

## 4. 特定区间警示 🟡 Major

### 4.1 chr5 qtl16 端粒伪峰风险
- 主候选 chr5 qtl16 的 `posMaxGprime = posPeakDeltaSNP = 25,021,533 bp`
- 正好位于 **chr5 的 Genoscope.12X 组装末端**（染色体末端 / 亚端粒区）
- 植物基因组端粒区通常**低重组率**，LD 扩展可能造成伪"宽峰"
- **因果变异可能位于峰值左侧 1-5 Mb 范围内**，而非峰值位置
- 建议：候选基因按距峰值距离分层报告（0-1 Mb / 1-3 Mb / 3+ Mb），不要仅在峰值位置找基因
- 可查 chr5 末端是否有组装 gap（参考 Genoscope.12X AGP 文件）

### 4.2 chr11 qtl5 建议剔除（v2.1 修订）
- Gprime q=0.05 下 `chr11:11,590-11,167,178` (11.16 Mb, 占 chr11 58%)
- **QTLseq 95% CI 方法下未获独立支持**（QTLseq 方法对窄峰更敏感, 未检出该区域）
- window=500 kb 敏感性分析下**仍显著但宽度依然 ~11 Mb, 未收缩成细窗**（提示信号是染色体级现象而非单一 QTL）
- 综合判断：**缺乏 QTLseq CI 与 w500kb 独立峰支持，建议从优先候选中剔除；疑似染色体级 stratification 或 LD 块**
- **建议客户将 chr11 qtl5 从候选列表剔除**（但保留在 Gprime q005 原始表供审计追溯）

### 4.3 chr12 qtl7 方向反向
- peakΔSNP = **−0.263**（HighBulk 贫化 Va-like 等位 = 富集 Vv-like）
- 仅为统计符号方向；**未做生物学验证**，可能含义:
  - Vv 在该位点**贡献对抗旱有利的等位基因**（多基因性状常见）
  - 或标注错位（需生物学验证）

---

## 5. 数据追溯 🟢 Minor

### 5.1 Pa 样本未纳入
- cohort VCF 有 `Pa` 样本（客户未说明身份），**本分析未纳入**亲本或混池
- 不影响当前结果，但后续若有 `Pa` 是隐性抗旱亲本的可能，结论需修订

### 5.2 5h 失水率表型元数据不全
- 客户仅提供 40 个体的样本编号 + 定性分类（抗旱 / 不抗），未提供：
  - 具体测定方法（叶片离体 / 烘干 / 重量法）
  - 失水率单位与阈值
  - 重复次数与测量日期
- 详见 `metadata/phenotype_source.meta.yaml`（本 release 内）

---

## 6. 不在本分析范围

以下内容**不在本次交付范围**，需客户自行或后续迭代（详见 `HOWTO_GO_KEGG.md`）：

- GO / KEGG 功能富集
- 与葡萄已知抗旱基因的文献比对 (VvDREB, VvMYB60, VvNCED3 等)
- 差异表达验证
- KASP 标记设计 + 扩群精细定位
- 分子克隆与功能验证

建议后续启动：
- `task-bsa-enrichment` — 候选基因功能富集（按 `HOWTO_GO_KEGG.md` 三方案之一）
- `task-bsa-report` — 客户汇报版报告（PDF）
- `task-bsa-fine-mapping` — chr5 qtl16 KASP 精细定位

---

## 7. 审计产出

本项目完整审计报告**内部留档**，不随 release 分发。审计覆盖：

- **第一轮 (2026-04-16)**: 项目整体审计（多 Agent auditor + Codex GPT-5.4），28 项发现，26 项已修复至 v2_fixed。
- **第二轮 (2026-04-17)**: Release 交付包独立审计（4 Agents），22 项发现，全部修复至 **v2.1**。

如客户需要审计细节，请联系分析方。
